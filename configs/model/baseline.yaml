# Baseline model configuration (no domain adaptation)

type: "baseline"
backbone: "mobilenet_v3_small"
pretrained: true
num_classes: 15 # 15 PlantVillage classes (Pepper, Potato, Tomato)
dropout: 0.5

# Freeze backbone layers (0 = train all)
frozen_stages: 0
